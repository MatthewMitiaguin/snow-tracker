# DynamoDB table name — created by terraform/
# Run: terraform -chdir=terraform output dynamodb_table_name
DYNAMODB_TABLE=snow-tracker-alerts

# Discord incoming webhook URL — alerts posted here when conditions are exceptional
# Leave blank to disable Discord notifications
DISCORD_WEBHOOK_URL=
